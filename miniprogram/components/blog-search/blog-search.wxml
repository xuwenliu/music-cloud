<view class="container">
	<van-search value="{{keyword}}" placeholder="请输入搜索关键词" use-action-slot bind:change="onInput" bind:search="onSearch" bind:cancel="clearKeyword">
		<view slot="action" bind:tap="onSearch">搜索</view>
	</van-search>
</view>