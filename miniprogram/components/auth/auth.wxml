<x-bottom-modal modalShow="{{modalShow}}">
    <view slot="auth-btn">
        <button open-type="getUserInfo" bind:getuserinfo="onGotUserInfo" size="mini" class="auth btn">
            获取微信授权信息
        </button>
    </view>
</x-bottom-modal>