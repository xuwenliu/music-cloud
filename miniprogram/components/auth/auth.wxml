<x-bottom-modal modalShow="{{modalShow}}">
    <view slot="modal-content">
        <van-button 
        open-type="getUserInfo" 
        bind:getuserinfo="onGotUserInfo" 
        color="#d43c43"
        round
        custom-class="auth">同意微信授权</van-button>
    </view>
</x-bottom-modal>